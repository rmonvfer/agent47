Lists files and directories in a given path. The path parameter must be absolute or relative to the working directory; omit it to list the current workspace directory. You should generally prefer the Find and Grep tools if you know which directories to search.